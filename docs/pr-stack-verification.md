# Stacked PR verification

## Warzone consumer

Fresh clean Java 25 verification after integrating foundation review fixes: {"tests":173,"failures":0,"errors":0,"skipped":0}. The nine existing duel advancements and keys remain unchanged. Consumer calls now pass the owning plugin; renderer dependency is pinned to pilot.4 at 1cefd9f. No deployed server or player data changed. Eight separate Node tooling tests also run before push.

## feature/commendation-advancements

Fresh local clean verification after synchronizing the reviewed parent: 187 Java tests, zero failures/errors/skips. Provider-specific progression is unchanged; owner-bound rendering, reliable retry, pinned companion builds, and the separate strict Codacy gate are inherited. This is not deployment or hosted approval.

## feature/express-advancements

Fresh local clean verification after synchronizing the reviewed parent: 197 Java tests, zero failures/errors/skips. Provider-specific progression is unchanged; owner-bound rendering, reliable retry, pinned companion builds, and the separate strict Codacy gate are inherited. This is not deployment or hosted approval.

## feature/diary-advancements

Fresh local clean verification after synchronizing the reviewed parent: 209 Java tests, zero failures/errors/skips. Provider-specific progression is unchanged; owner-bound rendering, reliable retry, pinned companion builds, and the separate strict Codacy gate are inherited. This is not deployment or hosted approval.
